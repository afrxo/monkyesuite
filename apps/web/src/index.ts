// @monkyesuite/web — TanStack Start SSR frontend (Cloudflare).
// Talks to the Railway API over HTTP ONLY; never touches the database.
// TanStack Start scaffolding lands in specs/08. Placeholder entrypoint.

export {};
