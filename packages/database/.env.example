# Migrations run as an admin/owner role (creates tables, policies, grants).
# Local dev default:
DATABASE_URL=postgres://davidboakye@localhost:5432/monkyesuite_dev

# Runtime roles (provisioned by drizzle/roles.sql), for reference:
#   APP role (RLS enforced)   — used by apps/api
# APP_DATABASE_URL=postgres://monkye_app:app_pw@localhost:5432/monkyesuite_dev
#   SERVICE role (RLS bypass) — used by apps/worker (pgx)
# SERVICE_DATABASE_URL=postgres://monkye_service:service_pw@localhost:5432/monkyesuite_dev
